1. Create a virtualenv
   python -m venv venv
   source venv/bin/activate

2. Install
   pip install -r requirements.txt

3. Run the API
   uvicorn app.main:app --reload --port 8000

4. Open dashboard
   http://localhost:8000/

5. Run device simulator (in another terminal)
   python device_simulator.py
